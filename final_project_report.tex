\documentclass[sigconf]{acmart}

\settopmatter{printacmref=false}
\renewcommand\footnotetextcopyrightpermission[1]{}
\pagestyle{plain}

\usepackage{graphicx}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}

\begin{document}

\title{CS 410 Final Project Report: A Comparative Study of Text-Based Spam Detection using TF-IDF, Word2Vec, and Structural Metadata}

\author{Nathan Xie (nathanx2), Vijwal Akula (vrakula2), Kangyi Jiang (kangyij2), Zaid Khan Mohammed
(zmoha7), Brian Cochran (brianac5)}
\affiliation{%
  \institution{University of Illinois Urbana-Champaign}
  \city{Champaign}
  \state{Illinois}
  \country{USA}
}

\begin{abstract}
Building upon our initial benchmark evaluation of corporate-to-internet cross-corpus domain shift, this final report presents a significantly enhanced, multi-faceted spam detection system. We implemented and benchmarked a comparative machine learning pipeline evaluating three distinct classification approaches: TF-IDF with Logistic Regression, Linear Support Vector Machines (SVM) with Bigram context, and Neural Word Embeddings (Word2Vec). By integrating structural metadata---including URL density, capitalization ratios, and punctuation frequency---alongside NLP-based lemmatization, we successfully addressed the feature sparsity issue identified in our milestone. Our enhanced Linear SVM model achieved an overall accuracy of 73.34\% on the out-of-distribution SpamAssassin corpus, while our Logistic Regression pipeline recorded a peak recall of 96.11\%. These empirical results demonstrate that combining phrase-level semantic context with explicit structural analysis yields a highly resilient defense against evolving spam strategies.
\end{abstract}

\keywords{Spam Detection, Natural Language Processing, TF-IDF, Word2Vec, Linear SVM, Domain Shift, Structural Feature Engineering}

\maketitle

\section{Introduction}
Despite continuous advancements in automated heuristic filtering, email remains a primary vector for cyberattacks, phishing, and unwanted promotional distribution. Traditional spam filters often rely on static blacklists or rigid rule-based systems, which malicious actors easily bypass by rotating sender domains and obfuscating vocabulary. 

The core challenge identified during our milestone analysis was "feature sparsity" caused by domain shift. When we trained a baseline unigram model exclusively on internal corporate communications (the Enron dataset) and tested it against general public internet traffic (the Apache SpamAssassin dataset), the model failed to generalize—yielding a 0.00 recall for the spam class. Malicious actors do not merely use different words across different environments; they employ entirely different linguistic and structural patterns.

For the final phase of our project, we focused on modeling "The Shape of Spam." Instead of relying purely on isolated keywords, our objective was to evolve the system to analyze the structural characteristics of emails and the deeper semantic context of phrases. This report details the successful deployment of a robust comparative system that benchmarks traditional linear models (Logistic Regression, SVM) enhanced with structural metadata against modern neural architectures (Word2Vec), ultimately proving that explicit structural context combined with n-gram representations offers superior generalizability.

\section{Methodology and Pipeline}
Our enhanced software architecture is implemented in Python, leveraging Scikit-learn for machine learning algorithms, Pandas for data manipulation, NLTK for linguistic normalization, and Gensim for neural embeddings. The pipeline is structured into three sequential stages: Data Preprocessing, Vectorization (TF-IDF and Word2Vec streams), and Classifier Modeling.

\subsection{Dataset Selection and Ingestion}
To simulate real-world conditions where a model must evaluate out-of-distribution (OOD) data, we maintain our cross-corpus evaluation strategy. The model is trained on the Enron Spam Dataset (corporate communications) and tested on the Apache SpamAssassin Public Corpus (wild internet traffic). 

Our custom recursive MIME parser traverses the nested structures of raw `.eml` files in the SpamAssassin corpus, seamlessly falling back to extracting HTML payloads when plain text is unavailable.

\subsection{Preprocessing and Lemmatization}
Raw email data contains immense structural noise. Our preprocessing module applies a sequences of normalizations:
\begin{enumerate}
    \item \textbf{HTML Stripping:} Residual HTML tag artifacts are removed.
    \item \textbf{Regex Normalization:} URLs are replaced with a generic "URL" token, and excessive non-essential characters are neutralized while preserving critical structural punctuation like exclamation marks and question marks.
    \item \textbf{Lemmatization:} To improve the model's ability to map identical concepts across different vocabularies, we integrated the NLTK \textit{WordNetLemmatizer}. This reduces varying word forms (e.g., "winning", "wins", "winner") to their semantic root ("win").
\end{enumerate}

\subsection{Structural Feature Engineering}
A core finding from our milestone was that spam emails exhibit a distinct structural shape independent of their content. We engineered a structural metadata extractor that computes 22 unique features. Key features include:
\begin{itemize}
    \item \textbf{URL Density:} The ratio of hyperlinks to the total word count.
    \item \textbf{Punctuation Intensity:} Specific counting of "!" and "?" which are heavily utilized in phishing urgency hooks.
    \item \textbf{Capitalization Ratios:} The percentage of all-caps tokens to identify "shouting" promotional emails.
    \item \textbf{Sender Domain Patterns:} A binary check for domain validity and character ratio analysis on the sender string.
\end{itemize}

\section{Exploratory Data Analysis (EDA)}
We generated comprehensive EDA plots to confirm the integrity of our processed, aggregated datasets (33,716 Enron training instances; 3,898 SpamAssassin testing instances).

\begin{figure}[h]
  \centering
  \begin{subfigure}[b]{0.48\linewidth}
      \includegraphics[width=\textwidth]{outputs/class_distribution.png}
      \caption{Class Balance}
      \label{fig:eda_dist}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.48\linewidth}
      \includegraphics[width=\textwidth]{outputs/text_lengths.png}
      \caption{Text Word Counts}
      \label{fig:eda_length}
  \end{subfigure}
  \caption{EDA of the integrated training and testing corpus.}
\end{figure}

As seen in Figure \ref{fig:eda_length}, text length remains a highly discriminative feature. Spam messages frequently exhibit a hyper-concentrated length distribution typical of automated templates, whereas legitimate human-typed ham emails show vast length variances.

\begin{figure*}[t]
  \centering
  \begin{subfigure}[b]{0.45\textwidth}
      \includegraphics[width=\textwidth]{outputs/top_terms_ham.png}
      \caption{Frequent Ham Tokens}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.45\textwidth}
      \includegraphics[width=\textwidth]{outputs/top_terms_spam.png}
      \caption{Frequent Spam Tokens}
  \end{subfigure}
  \caption{Lexical analysis of normalized email text.}
  \label{fig:lexical}
\end{figure*}

\section{Modeling Strategies}
We developed three distinct modeling pipelines to evaluate varying paradigms of text representation.

\subsection{TF-IDF Models (Logistic \& SVM)}
We upgraded our baseline Term Frequency-Inverse Document Frequency (TF-IDF) Vectorizer (restricted to a 5,000 max vocabulary) to analyze both unigrams and bigrams (\(ngram\_range=(1,2)\)). This allows the models to represent phrase-level dependencies (e.g., "bank account" or "act now"). 
\begin{itemize}
    \item \textbf{Logistic Regression:} Served as our enhanced baseline classifier.
    \item \textbf{Linear SVM:} Due to its capacity to find maximum-margin hyperplanes in high-dimensional feature spaces, LinearSVC handles sparse linguistic matrices exceptionally well.
\end{itemize}

\subsection{Word2Vec Neural Embeddings}
To contrast with the high-dimensional TF-IDF sparse matrices, we trained a Continuous Bag-of-Words Word2Vec model on our corpus using Gensim. This generates a dense 50-dimensional latent space where words spanning similar contexts are positioned in proximity. Entire documents are converted to fixed-length numeric vectors by calculating the mean centroid of their constituent word vectors. These representation vectors are then classified using a balanced Logistic Regression model.

\section{System Performance and Results}
We evaluated our final configurations via 5-fold cross-validation on the training set mapping and a final evaluation on the SpamAssassin out-of-distribution set, tracking Precision, Recall, Accuracy, and \(F_1\)-score (Table \ref{tab:results}).

\begin{table}[h]
\centering
\caption{Cross-Domain Performance Comparison (Test Set)}
\label{tab:results}
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & Accuracy & Precision & Recall & F1-Score \\ \midrule
Baseline (Milestone) & 64.16\% & 0.00 & 0.00 & 0.00 \\
\textbf{Logistic (Final)} & 71.99\% & 0.55 & \textbf{0.96} & 0.70 \\
\textbf{Linear SVM (Final)} & \textbf{73.34\%} & \textbf{0.57} & 0.92 & \textbf{0.71} \\
Word2Vec Centroid & 70.80\% & 0.53 & 0.88 & 0.67 \\ \bottomrule
\end{tabular}
\end{table}

During the 5-fold cross-validation on the internal Enron dataset, all models achieved near-perfect F1-scores (\(\sim 0.99\pm0.001\)). However, evaluating against the external SpamAssassin test set provided the true measure of their cross-domain generalizability.

\subsection{Confusion Matrix Analysis}
The addition of structural formatting features and lemmatization completely resolved our milestone failure. The progression is visualized in Figure \ref{fig:cm_comparison}.
\begin{figure*}[t]
  \centering
  \begin{subfigure}[b]{0.24\textwidth}
      \includegraphics[width=\textwidth]{outputs/tfidf_confusion_matrix.png}
      \caption{Milestone Baseline}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.24\textwidth}
      \includegraphics[width=\textwidth]{outputs/logistic_confusion_matrix.png}
      \caption{Final Logistic}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.24\textwidth}
      \includegraphics[width=\textwidth]{outputs/svm_confusion_matrix.png}
      \caption{Final SVM}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.24\textwidth}
      \includegraphics[width=\textwidth]{outputs/w2v_confusion_matrix.png}
      \caption{Word2Vec Neural}
  \end{subfigure}
  \caption{Confusion matrix comparison highlighting the recovery of spam detection capabilities.}
  \label{fig:cm_comparison}
\end{figure*}

Our final \textbf{Logistic Regression} model emerged as an exceptionally sensitive filter, minimizing false negatives and achieving an outstanding 96.11\% spam recall. \textbf{Linear SVM} provided the highest comprehensive accuracy (73.34\%) by drawing a cleaner, wider decision boundary, suppressing the false-positive rate compared to the logistic thresholding. Though \textbf{Word2Vec} presented an improvement over our baseline, it failed to surpass the TF-IDF approaches—demonstrating that predicting spam across domains relies more heavily on literal sparse indicators (e.g., explicit phrase occurrence) than broad semantic similarities.

\subsection{Precision-Recall Trade-offs}
For real-world deployment, systems must balance aggressive spam filtration against the unacceptable risk of discarding legitimate correspondence.

\begin{figure*}[t]
  \centering
  \begin{subfigure}[b]{0.32\textwidth}
      \includegraphics[width=\textwidth]{outputs/logistic_pr_curve.png}
      \caption{Logistic (TF-IDF)}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.32\textwidth}
      \includegraphics[width=\textwidth]{outputs/svm_pr_curve.png}
      \caption{SVM (TF-IDF)}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.32\textwidth}
      \includegraphics[width=\textwidth]{outputs/w2v_pr_curve.png}
      \caption{Word2Vec (Neural)}
  \end{subfigure}
  \caption{Precision-Recall curves evaluating decision threshold margins.}
  \label{fig:pr_comparison}
\end{figure*}

As shown via the Precision-Recall curves in Figure \ref{fig:pr_comparison}, the Linear SVM (Figure 5b) maintains consistent Area Under the Curve (AUC) stability. It retains a respectable level of precision without undergoing a severe dropoff as recall is aggressively scaled, granting administrators reliable margins when configuring production spam thresholds.

\section{Conclusion}
Our final project implementation successfully transitions from a brittle keyword matcher subject to severe domain shift into a structurally aware, generalizable detection engine. Through comparative benchmark testing, our hypothesis is affirmed: while Neural Embeddings offer impressive dense context mapping, a computationally efficient, highly-dimensional Bigram TF-IDF space enriched with explicitly engineered structural indicators remains the optimal architecture for generalized spam filtering.

\end{document}
